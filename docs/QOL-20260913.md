# Meta toggle and fullscreen panel access freeze

Installed and physically accepted by J; authorized for publication to main.
The panel sends the launcher's D-Bus toggle on
repeat activation; ordinary close releases the Kadunce guest. Initial invocation
requests panel focus through Plasma's AcceptingInput lifecycle, then the launcher's
existing layer-shell overlay takes focus. The fullscreen window is not minimized
or moved. Seven CTest checks and source checks pass, including private-bus repeat
activation and panel status reset. A running fixture is not killed or duplicated.

J reports a full pass: Meta opens/closes repeatedly; Meta over a fullscreen app
exposes the launcher and usable dock; close returns focus normally. Physical
acceptance supplements the offscreen/private-bus tests.

Installed SHA-256 identities, verified before freeze:

- Launcher: `a947545e571bc3e333301ebf3a80d3c2847424caf8b7fe5597e02ead37f0902e`
- Panel plugin: `eb0c7f13850e5bf0995204177404600b26de9ed968fb55ad5d88561587f87309`

The local combined QoL installer also contains Kadunce's shared Active-entry
elevation cleanup for Temperance popup visibility. Temperance source is unchanged.
Rollback restores all three previous binaries; loading plugins requires logout/login.
