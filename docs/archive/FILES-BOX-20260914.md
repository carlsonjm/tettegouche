# Mouse box selection

J accepted mouse empty-space drag-box selection. Local checkpoint after main 035b60c.

Drag from empty grid space to select intersected cells. Plain drag replaces,
Shift adds, Ctrl toggles against the selection at press. Release retains the
selection. Grab cancellation restores the original selection. A quiet translucent
rectangle uses the established neutral outline. Selection does not change the
clipboard or files. Backend filters paths against the visible listing and deduplicates.

MouseArea rejects synthesized touch and presses on entries. Existing touch
tap/double-tap/hold and scrolling paths remain unchanged. Item drag/drop and
edge-autoscroll during box selection are not included in this bounded pass.

Tests: QML box replace/add/toggle plus existing touch and mouse controls;
native backend listing validation, deduplication and selection clearing.

Candidate SHA256: 607541b61e843e024d47e1c87f95c17e66f40904bd081e8dde13b53ac1cc58b0.
Installer: install-tette-files-box-20260914.sh in workspace root. Rollback:
accepted 0457a932 binary, corresponding to main 035b60c.
